<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
<% if signed_in? %>
	<% if admin? %>
  	<%= render @users %>
  <% end %>
<% else %>
	<%= link_to "Sign in", signin_path %>
<% end %>
  
</ul>

